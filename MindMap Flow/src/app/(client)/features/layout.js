export const metadata = {
  title: "Tính năng - Mindmap Flow",
  description: "Tính năng - Mindmap Flow",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
