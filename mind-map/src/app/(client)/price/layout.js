export const metadata = {
  title: "Bảng giá - Mindmap Flow",
  description: "Bảng giá - Mindmap Flow",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
